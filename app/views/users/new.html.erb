<% if current_user %>
  <% provide(:title, 'Create New User') %>
  <%= render 'form', :legend => 'Create New User', :user => @user %>
<% else %>
  <% provide(:title, 'Sign Up') %>
  <%= render 'form', :legend => 'Sign Up', :user => @user %>
<% end %>
